# Зависимости времени выполнения IVTrace.
# pytest — не только для разработки: он нужен для предполётных самотестов,
# которые программа гоняет перед каждым измерением (защита оборудования).
#
# Версии запинены диапазоном «проверенная … < следующий мажор»: нижняя
# граница — то, на чём код реально проверен, верхняя отсекает мажорные
# релизы с ломающими изменениями (баг-репорт: незапиненный pandas тянул 3.x,
# из-за чего экспорт XLSX падал на подборе ширин колонок — см. analysis.py).
# Диапазон, а не ==, чтобы установка из исходников на macOS/Linux не упиралась
# в отсутствие колёс под конкретную патч-версию.
pyvisa>=1.14,<2
pyserial>=3.5,<4
pandas>=2.2,<4
numpy>=1.26,<3
matplotlib>=3.8,<4
openpyxl>=3.1,<4
pytest>=8,<10
