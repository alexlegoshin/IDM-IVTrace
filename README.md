# IDM-IVTrace
IDM+ IVtrace automates the acquisition of current–voltage (I‑V) characteristics of current sensors. It controls a unipolar source and a multimeter via SCPI (pyvisa), operates in pulsed mode (output on/off per point), caches measurement parameters in a JSON file, and saves detailed CSV files with metadata headers.
