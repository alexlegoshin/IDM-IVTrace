# IVTrace v2.0 — план работ

Статус: **в работе, Ф1**. Ветка `v2-dev` (отведена от `main` = v1.4ae).
Ход работ — в конце документа (§6).

Документ описывает, как из v1.4ae + ветки `test_deepseek_hermes` собрать v2.0
по 40 пунктам ТЗ (36 исходных + 37-40, поступившие по ходу обсуждения).
Порядок пунктов в ТЗ ≠ порядок реализации: ниже они
перегруппированы по зависимостям, потому что половина пунктов трогает один и
тот же код и делать их вразнобой = переписывать одно и то же по три раза.

---

## 0. Что уже выяснено (разведка проведена до составления плана)

### 0.1. Ветка `test_deepseek_hermes` — что в ней есть и в каком состоянии

Один коммит `c123a49` поверх `7ff524e` (это ещё до RIGOL/autorange/X=0).
Внедрено: остановка по погрешности, инверсия входа, отключение реле,
профили датчиков (`SensorConfigManager`).

**Прогнан полный тест-сьют прямо на ветке: 12 из 92 тестов падают.**
Ветка не готова к слиянию как есть, требует доработки при мёрдже:

| Проблема | Где | Что делать при мёрдже |
|---|---|---|
| `run_measurement()`/`_measure_branch()` стали возвращать кортеж `(results, aborted_reason)` — все старые вызовы и тесты падают с `TypeError` | `measurement.py` | оставить кортеж (он нужен), но обновить тесты; в v2 всё равно возвращаем структуру богаче |
| `resolve_measure_params()` **безусловно** требует `I_nom`/`ratio` и лезет в `input()` — зависание на stdin в скриптах и CI | `cli.py:275` | сделать опциональными; спрашивать только если реально нужны (включена отсечка по погрешности) |
| `'use_relay': not args.no_relay if args.no_relay is not None else ...` — `store_true` никогда не даёт `None`, загруженный из конфига `use_relay: False` всегда затирается на `True` | `cli.py` | переписать на трёхзначную логику (см. п. 8) |
| `I_nom` протащен через все сигнатуры, но нигде не читается — в формуле погрешности участвует только `ratio` | `measurement.py`, `orchestrate.py` | либо задействовать (нормировка погрешности), либо убрать |
| Тесты не тронуты вообще (`git diff` по `tests/` пустой) | — | новые фичи покрыть тестами при мёрдже, а не «потом» |

**Уточнение по п. 22.** Проверил: ветка `test_deepseek_hermes` построение
графика **не убирала** — вкладка «График» и `analyze` целы, диффа по ним нет.
Но содержание п. 22 от этого не теряет смысла: график сейчас строится
**только вручную** (кнопка/подкоманда), автоматически по окончании измерения
он не появляется. Это и надо сделать в Ф3, не сломав ручной режим (п. 20).

**`invert_input` — по указанию заказчика меняет назначение.** Сейчас он на лету
инвертирует знак `X_set` при записи, т.е. подделывает первичные данные. В v2:
переносится из измерительного цикла в **пост-обработку отдельного CSV**
(отдельная операция над уже снятым файлом, с пометкой в мете), и в UI/доке
явно помечается как нежелательный к использованию.

### 0.2. Что забрать из соседних репозиториев

**`IDM-DTCal`** — уже содержит рабочие, **проверенные вживую** конфиги
мультиметров в режиме **вольтметра**. Это готовый фундамент для п. 4:

- `akipb778.json` — шкалы DCV **0.1/1/10/100/1000 В** (платформа Agilent 34401A).
  В комментарии прямо описан баг: раньше стоял чужой массив 0.2/2/20/200/1000,
  из-за чего запрос диапазона 20 В прибор округлял вверх до 100 В и терял
  разрешение. **Это прямая иллюстрация к п. 6** — неверный массив `ranges`
  ломает ручное ранжирование молча, без ошибок.
- `rigol_dm3068.json` — **ключевая находка**: DM3068 ручной диапазон
  поддерживает, просто через другую подсистему. Не `SENS:...RANG {value}`, а
  `:MEASure:VOLTage:DC {index}` — **индексом шкалы (0..4)**, и эта команда сама
  уводит прибор в Manual. Авто — `:MEASure AUTO`. То есть наш вывод в v1.4ae
  «DM3068 не умеет ручной диапазон» был неполным: он не умеет *нашего
  синтаксиса*. В v2 конфиг должен поддерживать оба стиля (`{range_val}` и
  `{index}`).

**`IDM-DNKMetr`** — README это подробный отчёт о вводе стенда в эксплуатацию.
Оттуда для п. 5 (источник напряжения GPP-4323):

- Подключение — **не USB-TMC, а ASRL (COM), 115200 8N1**, и только после
  установки вендорского драйвера GW Instek (устройство vendor-specific
  `VID_2184&PID_0057`, обычный CH341SER не подходит, Код 28 в диспетчере).
- **Включение выхода — `ALLOUTON`/`ALLOUTOFF`, проверено вживую.** У нас в
  `instruments/voltage_sources/gpp74323.json` в качестве `output_on` стоит
  `":OUTPut{ch}:STATe ON"` — не проверено ни разу. `ALLOUTON`/`ALLOUTOFF` в
  нашем конфиге лежат рядом, но код их не использует. **Вероятнее всего именно
  это и есть причина, по которой промеры датчиков напряжения не работают.**
- `VOUT1?`/`IOUT1?` отвечают **с суффиксом единиц** (`"00.000V"`, `"0.0000A"`) —
  голый `float()` на таком ответе падает, нужен парсер числа регуляркой.
  У нас readback вообще не реализован, но при добавлении сразу сломался бы.
- Потолок 64 В паспортный, рабочий ограничитель в DNKMetr — **60 В**.
- Долгое нажатие F6 на передней панели отключает дистанционное управление;
  только один интерфейс (USB/RS-232/LAN) активен одновременно.

**`IDM-DTCal`, релиз `v1.1-bipolar-fix`** — для п. 31 и п. 35: биполярная
выходная характеристика `OutputScale(v_minus, v_zero, v_plus)`, кусочно-линейная
с изломом в нуле, нормировка погрешности на **размах** `V_plus − V_minus`.

### 0.3. Разбор бага ручного ранжирования (п. 6) — найден, и он не один

`instruments.py:56-83`, `Multimeter.auto_range()`. Пять дефектов:

1. **Диапазон выбирается по ПРЕДЫДУЩЕЙ точке.** `measurement.py` вызывает
   `dmm.auto_range(i_avg)` *после* измерения точки N, и результат применяется
   к точке N+1. На шаге 0→50 А (датчик 1:2000, ~25 мА) диапазон, подобранный
   под нулевой ток, физически не годится. Это первопричина `9·10³⁸`
   ([issue #3](https://github.com/alexlegoshin/IDM-IVTrace/issues/3)).
2. **Подъём только на одну ступень за точку** (`current_range_idx += 1`).
   Даже поймав переполнение, алгоритм не может догнать за один шаг —
   с 200 мкА до 20 мА это 2 точки мусора подряд.
3. **Сентинел переполнения скармливается обратно в алгоритм.** `9.9e37`
   приходит в `auto_range()` как «измеренный ток» — состояние диапазона
   после этого бессмысленно. Нет проверки `abs(v) > 1e30`.
4. **Гистерезис задан так, что провоцирует автоколебания.** Подъём при >95%
   предела, спуск при <10%. На декадных шкалах 10% диапазона N — это ровно
   100% диапазона N−1, то есть сразу >95% → колебание вверх-вниз на границе.
5. **Нет паузы после смены диапазона** — прибор физически переключает реле
   входного делителя и не успевает устояться. Отсюда требование п. 6 о
   задержке 0.7 с.

Плюс шестое, конфигурационное: неверный массив `ranges` ломает всё молча
(см. историю DTCal выше).

### 0.4. Прочие подтверждённые находки

- **п. 31**: `analysis.py:91` — `df['Error_percent'] = np.abs(...)`. Модуль
  берётся буквально в одной строке, править точечно.
- **п. 35**: `max_voltage: 64.0` в конфиге GPP есть, но **нигде не проверяется**
  в коде. Ограничения источника сейчас не enforced вообще.
- **п. 28**: ограничений по току реле в коде нет ни в каком виде.

---

## 1. Решения по спорным местам ТЗ

Все пять вопросов заданы заказчику и **закрыты** (согласовано 2026-08-05).
Решения зафиксированы ниже как обязательные к исполнению.

### В-1. Куда ставить программу (п. 1) — ✅ РЕШЕНО: вариант ниже утверждён

ТЗ: «минимизируем зависимость от админских прав», «ПО должно работать без
админских прав», и одновременно «ставиться в `C:/Program Files/...` и **там
хранить все конфиги, csv и графические темп-файлы**».

Так нельзя: запись в `Program Files` без прав администратора невозможна.
Программа установится (инсталлятор один раз попросит UAC), но при первом же
сохранении CSV упрётся в отказ доступа — либо молча уедет в
`VirtualStore` (Windows UAC-виртуализация), где пользователь их не найдёт.

**Рекомендую** (и заложу в план по умолчанию): раздельные пути, стандарт Windows —

| Что | Куда | Права |
|---|---|---|
| exe, `_internal/`, эталонные конфиги приборов | `%LOCALAPPDATA%\Programs\Legoshi\IDM\IVTrace` | **не нужны** |
| конфиги приборов (правленые), настройки приложения | `%LOCALAPPDATA%\Legoshi\IDM\IVTrace\config` | не нужны |
| CSV/XLSX/PNG — рабочая папка | по умолчанию `%USERPROFILE%\Documents\IVTrace`, настраивается из UI (п. 23) | не нужны |
| кеш, temp-графика | `<рабочая папка>\Cache` (по п. 23) | не нужны |

Установка целиком в пользовательский профиль → **инсталлятор вообще не просит
UAC**, автообновление (п. 2) работает без прав, что для апдейтера критично.
Вариант «Program Files для бинарников + LOCALAPPDATA для данных» тоже рабочий,
но требует UAC на установку и на каждое обновление.

### В-2. п. 3 — метаданные поверки в файлы результата — ✅ РЕШЕНО

Догадка подтверждена. В шапку CSV и на лист «Мета» XLSX пишется по каждому
использованному прибору:

```
# Прибор: <модель>, S/N <серийный>, VISA <ресурс>
#   поверка: <дата последней>, следующая: <дата следующей>
```

То есть **три факта на прибор**: что использовался, когда поверен, когда
следующая поверка. Дата следующей вычисляется из даты последней + межповерочный
интервал (оба поля — в JSON прибора).

### В-3. п. 19 — направления промера — ✅ РЕШЕНО

Пресеты с картинкой-схемой утверждены (вместо 8 галочек). Направление — это
три независимые оси, а не 4 варианта:

1. **направление внутри ветви**: от нуля к максимуму (`0→100`) или от максимума
   к нулю (`100→0`);
2. **порядок ветвей**: сначала «+», потом «−», или наоборот;
3. **стык ветвей**: ветви «расходятся от нуля» (`0→+100`, затем `0→−100`) или
   **«сходятся в нуле»** (`+100→0`, затем `0→−100`) — второй режим и есть
   непрерывный проход через ноль без скачка, в DTCal он сделан специально,
   чтобы не перемагничивать датчик (там прямо в описании репозитория:
   *branches meet at zero to respect sensor magnetization*).

2×2×2 = 8 комбинаций. В UI — **именованные пресеты с картинкой-схемой**.

**Обязателен к включению пресет `+100→0, затем 0→−100`** — непрерывный проход
через ноль без скачка (ветви «сходятся в нуле»). Это прямое требование
заказчика; в DTCal он же сделан специально, чтобы не перемагничивать датчик.

Предварительный список пресетов (финализируется в Ф2):

| Пресет | Схема | Зачем |
|---|---|---|
| Расходящийся от нуля | `0→+100`, затем `0→−100` | текущее поведение v1.4ae |
| **Сходящийся через ноль** | `+100→0`, затем `0→−100` | **обязательный**, непрерывный проход без скачка |
| Спад от максимума | `+100→0`, затем `−100→0` | обе ветви от максимума |
| Полный гистерезисный цикл | `0→+100→0→−100→0` | снятие петли гистерезиса |

### В-4. п. 10 — округление коэффициента — ✅ РЕШЕНО

Шаг 50 как в ТЗ. Рядом с округлённым значением **всегда показывается
фактическое и процент расхождения**, чтобы оператор видел, если округление
притянуто за уши (для малых коэффициентов шаг 50 грубоват: между 1:50 и 1:100
ничего нет).

**Автоматическое определение коэффициента помечается BETA** — по решению
заказчика. В UI и в доке — явная пометка.

### В-5. п. 28 — источник тока — ✅ РЕШЕНО

Источник на стенде: **АКИП-1162-10-1020** (госреестр СИ 92879-24),
1 канал, **10 В / 1020 А**.

Два следствия, оба важные:

1. **Источник физически способен выдать 1020 А — больше, чем выдерживает
   реле (800 А).** То есть программный лимит п. 28 не «на всякий случай», а
   единственное, что стоит между оператором и сгоревшим реле. Реле —
   связывающее ограничение, а не источник.
2. **Потолок по напряжению всего 10 В**, а `V_limit` сейчас не проверяется
   вообще. Задание `V_limit` больше 10 В физически недостижимо и должно
   отсекаться на вводе.

Конфиг `instruments/current_sources/` получает поля `max_current` (1020.0),
`max_voltage` (10.0), корректные `model_name`/`keywords` и номер в госреестре.
Набор SCPI-команд менять не нужно: АКИП-1162 — это ребрендинг ITECH IT-M,
команды в текущем конфиге те же (в DTCal файл так и называется
`akip1162.json`, а внутри — ровно наш ITECH-профиль).

---

### Дополнения к ТЗ, поступившие по ходу (пп. 37-39)

#### п. 37 — Число витков через датчик · Ф2, критично

Датчик на 2000 А промеряется несколькими витками: источник даёт 500 А,
через окно датчика проходит 4 витка, датчик «видит» 2000 А. Вводится параметр
**`turns`** (по умолчанию 1).

- Реальный вход датчика = **`X_set × turns`**.
- В файл пишутся **все три величины**: уставка `X_set`, число витков,
  итоговый вход `X_set × turns`.
- **График строится от реального входа `X_set × turns`, а не от уставки.**
- Ожидаемый выход в отсечке по погрешности (п. 7) и в определении
  коэффициента (п. 10) считается тоже от реального входа.

**Взаимодействие с п. 28 — здесь легко ошибиться в опасную сторону.**
Через реле и источник течёт **`X_set`**, а не `X_set × turns`: витки
умножают ампервитки внутри датчика, а не ток в проводе. Значит:

- лимиты реле (400 А предупреждение / 800 А запрет) проверяются по **`X_set`**;
- проверять их по `X_set × turns` было бы неверно — запретило бы штатный
  промер 2000 А датчика четырьмя витками при совершенно безопасных 500 А
  в проводе. Витки для того и нужны.

Для датчиков напряжения витки смысла не имеют — параметр показывается
только при возбуждении током.

#### п. 38 — Галочка «Отключить все предупреждения и уведомления» · Ф5

По аналогии с существующей «Игнорировать самотесты (не рекомендуется)».
Гасит: предупреждение >400 А (п. 28), уведомления о поверке (п. 3),
уведомление об ошибке ориентации датчика (п. 14), прочие всплывающие
предупреждения.

**Что галочка НЕ отключает ни при каких условиях** (иначе она превращается
из «не мешай мне работать» в «отключить технику безопасности»):

- **жёсткий запрет 800 А** — это не предупреждение, а запрет;
- аварийный останов (п. 32);
- отсечку по погрешности (п. 7) — она включается отдельной галочкой и
  является функцией измерения, а не уведомлением.

#### п. 39 — Довести `SensorConfigManager` до ума · Ф2/Ф5

Сейчас (как приехало из ветки) профили датчиков полурабочие:

| Проблема | Следствие |
|---|---|
| `list_sensor_configs()` реализован, но **GUI его не вызывает** — имя конфига вводится руками в текстовое поле | оператор должен помнить имена наизусть; опечатка = «конфиг не найден» |
| Хранятся в **рабочей** папке (`data/sensor_configs/`) | по В-1 рабочая папка настраиваемая и полна CSV — конфигам там не место |
| Имя файла не валидируется | `load_sensor_config("../../..")` уходит за пределы папки |
| Нет метаданных (когда сохранён, чем, для какого датчика) | через полгода не разобрать, что за файл |
| Удаление есть в коде, но не в UI | мусор копится |

Делаем:

1. **Переезд в конфигурационную папку** (по В-1):
   `%LOCALAPPDATA%\Legoshi\IDM\IVTrace\config\sensors\`. Рабочая папка
   остаётся под результаты, конфиги оператор случайно не тронет.
2. **Раскладка, чтобы не перемешивались** — подпапки по типу возбуждения:
   ```
   config/sensors/
   ├── current/     # датчики тока
   └── voltage/     # датчики напряжения
   ```
   При загрузке показываются только профили, подходящие к текущему типу
   возбуждения — профиль датчика напряжения физически не может быть применён
   к промеру током (там витки, ratio в А/А, другие шкалы).
3. **Выпадающий список вместо текстового поля** + кнопки Сохранить / Загрузить
   / Переименовать / Удалить. Имя вводится только при сохранении нового.
4. **Валидация имени файла** — только безопасные символы, никакого выхода за
   пределы папки.
5. **Метаданные в профиле**: имя, дата сохранения, версия IVTrace, комментарий.
6. Один профиль = один датчик и его режим промера целиком (ratio, I_nom,
   turns, диапазон, шаг, задержки, усреднение, направление). Если на практике
   окажется, что один датчик мерят разными режимами, — разделим на «профиль
   датчика» и «режим измерения» двумя списками; пока это преждевременно.

#### п. 40 — Режим отладки: прямая уставка значения · Ф4 (и CLI, Ф5)

Расширение ручного управления реле (п. 13) до полноценного отладочного
режима. Оператор задаёт **одно значение со знаком** — например `−25 В` — и
программа сама:

1. извлекает знак;
2. ставит реле в соответствующее положение (`IFW`/`IRW`);
3. подаёт на источник **модуль** значения (источник отрицательное не умеет);
4. держит, пока не сказано снять.

На датчик приходит −25 В, источник при этом выдаёт +25 В. Оператору не надо
думать про реле вообще.

Два режима в отладочной панели, оба нужны:

| Режим | Что делает оператор | Когда нужен |
|---|---|---|
| Ручное реле (п. 13) | сам выбирает положение реле и фиксирует его | когда руками что-то перекоммутируешь на стенде |
| **Прямая уставка (п. 40)** | вводит значение со знаком, реле и источник настраиваются сами | быстрая проверка датчика в конкретной точке без полного прогона |

**Обязательно и в GUI, и в CLI** — заказчик отметил это как критичное.
В CLI это отдельная подкоманда (например `set`), которая по сути одноразовый
скрипт, но внутри большой программы должна пройти те же проверки, что и
измерение: лимиты п. 28, самотесты, аварийный останов по Ctrl+C.

Зависимость: логика «знак → положение реле + модуль на источник» — та же,
что в планировщике развёртки (А-2, п. 17). Реализуется **после** Ф2, чтобы
не написать её дважды и по-разному.

## 2. Сквозные архитектурные изменения

Это не отдельные пункты ТЗ, а то, без чего пункты не сделать чисто. Делается
в Ф1-Ф2 и дальше используется всеми.

### А-1. Ось «тип выхода датчика» (для п. 4, 35)

**Частично сделано в Ф1** (см. §6): `Multimeter` умеет роль вольтметра
(`measure()`/`measure_voltage()`, конфиги в `multimeters_voltage/`) как
самостоятельная, протестированная возможность класса. Но измерительный
цикл (`measurement.py`) её пока не использует — он по-прежнему жёстко
предполагает, что выход датчика — **ток** (`dmm.measure_current()`, колонка
`I_meas_A`). Ниже описана полная ось, которую предстоит достроить в Ф2/Ф3:
вводится вторая ось, независимая от типа возбуждения:

|  | выход: ток | выход: напряжение |
|---|---|---|
| **возбуждение: ток** | есть сейчас | п. 4 |
| **возбуждение: напряжение** | п. 5 (чинится) | п. 4 + 5, BETA |

Следствия: `Multimeter` получает режим (`current`/`voltage`) и грузит
соответствующий блок конфига; колонка результата становится `Y_meas` с
отдельной колонкой единиц; конфиги мультиметров получают **два** набора
команд/шкал. Конфиги из DTCal переносятся почти как есть.

### А-2. Планировщик развёртки (для п. 8, 17, 18, 19)

Сейчас развёртка считается прямо в цикле:
`num_steps = int(round((X_stop - X_start) / X_step)) + 1`, знак навешивается
параметром `sign`. Этого не хватит ни для одного из пунктов 8/17/18/19.

Вводится **чистая функция-планировщик** без железа:

```
plan_sweep(X_start, X_stop, X_step, branches, direction_preset, ...)
    -> [SweepPoint(x_set, branch, relay_state, is_zero, is_endpoint), ...]
```

Вся комбинаторика ввода, обход нуля, обязательные точки (0 и |конец|),
порядок ветвей и направления живут здесь. Измерительный цикл становится
тупым исполнителем плана. **Это ключевое решение всего v2**: планировщик
тестируется сотней быстрых юнит-тестов без приборов, а измерительный цикл
перестаёт быть местом, где прячутся баги комбинаторики.

### А-3. Единый «результат точки» вместо голого float (для п. 7, 9, 14, 29)

Точка перестаёт быть числом. `MeasuredPoint` несёт: все N сырых отсчётов,
какие отброшены и почему (первый по п. 29, переполнение, повтор по п. 9),
среднее, СКО, использованный диапазон, знаковую погрешность (п. 31), флаги
(«переполнение», «инверсия полярности» п. 14, «превышен порог» п. 7).
CSV/XLSX и график берут из этой структуры то, что им нужно.

### А-4. Служба приборов, отвязанная от измерения (для п. 11, 12, 13, 25, 3)

Поиск приборов, их состояние, статус поверки, blink, ручное управление реле
выносятся в отдельный асинхронный сервис с собственным жизненным циклом,
не привязанный к запуску измерения. UI подписывается на его события.

### А-5. Пути и рабочая папка (для п. 1, 2, 23)

`apppaths.py` расширяется: `install_dir` / `config_dir` / `work_dir` / `cache_dir`
(см. В-1). Всё, что сейчас лезет в `data/`, идёт через эти функции.

---

## 3. Фазы

Зависимости строгие: фаза N не начинается, пока не закрыта N−1, **кроме**
явно отмеченных «можно параллельно».

### Ф0 — Мёрдж и стабилизация · пункты: (ветка deepseek)

Цель: одна ветка `v2-dev`, зелёный тест-сьют, ничего не потеряно.

1. Мёрдж `test_deepseek_hermes` в `v2-dev`. Конфликты ожидаются в
   `measurement.py` и `orchestrate.py` (там наши правки X=0 и autorange).
2. Починить 12 падающих тестов + 4 дефекта ветки из таблицы 0.1.
3. `invert_input` вынуть из измерительного цикла (станет п. 26-adjacent
   пост-обработкой в Ф4), в цикле — удалить.
4. Вернуть постройку графика в цикл (задел под п. 22).
5. `SensorConfigManager` оставить как есть — он в v2 пригодится.

Выход: `pytest` зелёный, ручной прогон GUI на железе не хуже v1.4ae.
**Риск:** низкий. **Оценка:** 1 подход.

---

### Ф1 — Приборы и безопасность · пункты 4, 5, 6, 28, 32, 35(частично), 3

Самая важная фаза: это железо и техника безопасности. Всё остальное строится
поверх и без этого не имеет смысла.

| № | Что | Ключевое |
|---|---|---|
| **32** | Аварийный останов | **Делается первым.** Жёсткая последовательность без таймаутов: выход источника OFF → реле разомкнуть (не вышло — дропнуть порт) → ток/напряжение в 0 → разрыв связи со всеми приборами. Каждый шаг в своём `try/except`, ни один не блокирует следующий. Отдельный тест на «прибор не отвечает на каждом из шагов». |
| **28** | Лимиты тока реле | 800 А — жёсткий запрет (валидация ввода, не доводить до железа). >400 А — предупреждение с текстом про мануал производителя и снятие ответственности. Плюс поле предела в конфиг источника (см. В-5). |
| **6** | Autorange/ручной + баг | Чинятся все 5 дефектов из 0.3: выбор диапазона по **ожидаемому** значению следующей точки (`X/ratio`), а не по предыдущему замеру; прыжок сразу на нужный индекс; детект сентинела `>1e30` → NaN + повтор на максимуме; гистерезис по границе соседней шкалы, а не 10%; настраиваемая задержка после смены диапазона (**0.7 с по умолчанию**). Конфиг: поддержка `{range_val}` и `{index}`-стиля (DM3068). |
| **4** | Мультиметр как вольтметр | ✅ Сделано в Ф1: конфиги + класс умеют роль вольтметра (см. §6). Полная ось А-1 (тип выхода датчика как параметр измерительного цикла, а не только возможность класса) — Ф2/Ф3, как и планировалось изначально. |
| **5** | Починка источника напряжения | `ALLOUTON`/`ALLOUTOFF` вместо `:OUTPut{ch}:STATe`; парсер числа с суффиксом единиц; ASRL 115200 8N1 в конфиге; enforce потолка 60 В; readback `VOUT1?`/`IOUT1?`. Пометка **BETA** (п. 35). |
| **3** | Поверка приборов | Поле даты поверки в JSON приборов + интервал. При подключении: <3 мес — жёлтое предупреждение, просрочено — **красный баннер «ПРИБОР НЕ ПОВЕРЕН»**. Даты поверки пишутся в шапку CSV/мету XLSX (см. В-2). Редактор конфигов в UI — в Ф5 (п. 3-UI). |

**Риск: высокий** — тут железо, и часть (п. 5) вообще никогда не работала.
Нужны прогоны на стенде после каждого пункта.
**Оценка:** 3-4 подхода. Ф1 частично параллелится: 32/28 независимы от 4/5/6.

---

### Ф2 — Ядро измерения · пункты 8, 17, 18, 19, 29, 9, 7, 14, 27

Строится на планировщике А-2 и структуре точки А-3. Всё это — один
взаимосвязанный кусок, дробить его нельзя.

| № | Что | Ключевое |
|---|---|---|
| **17** | Универсальный ввод | Планировщик А-2 принимает любые `X_start`/`X_stop` (`−250→250`, `0→250`, `250→−250`, `−25→25`, …) и раскладывает их на ветви + состояния реле. Источник не умеет отрицательный ток — знак **всегда** отрабатывается реле, никогда источником. **Ноль добавляется, только если развёртка через него реально проходит** — при `150→250` ноль не трогаем вообще (уточнено заказчиком). Если проходит — ноль ставится, даже когда не попадает на шаг. Модуль конечной точки — так же всегда. |
| **37** | Витки через датчик | Реальный вход = `X_set × turns`, в файл пишутся все три величины, график и расчёты — от реального входа. Лимиты п. 28 проверяются по `X_set`, **не** по произведению (см. пояснение в §1). |
| **39** | Профили датчиков | Довести `SensorConfigManager` (см. §1): переезд в конфигурационную папку, подпапки по типу возбуждения, выпадающий список, валидация имён, метаданные. Логика — здесь, UI-часть — в Ф5. |
| **18** | Баги шага | Проверяется на планировщике юнит-тестами: шаг через ноль, шаг не кратный диапазону, `X_step` больше диапазона, накопление ошибки float на длинных развёртках (сейчас `int(round(...))` + умножение — на 0.1-шагах даст дрейф). |
| **8** | Выбор ветви | Не bool `use_relay`, а перечисление **`+` / `−` / `both`** (третье значение — «без реле», если оператор коммутирует руками). Заменяет кривой `use_relay` из ветки deepseek. |
| **19** | Направления промера | Пресеты направлений (см. В-3), реализуются как параметр планировщика. |
| **29** | Усреднение | Настраиваемое число отсчётов (**по умолчанию 4**), задержка между ними (по умолчанию 0), **отбрасывание первого отсчёта** (по умолчанию вкл). |
| **9** | Контрольные промеры | При отклонении сверх порога — ещё 2 попытки (итого 3). Стабильно мимо → точка бракуется и **не идёт в расчёт**, но остаётся в сырых данных с флагом. |
| **7** | Отсечка по погрешности | Переработка того, что пришло из ветки deepseek: галочка вкл/выкл, работает **после** механики п. 9 (три попытки → и только потом решение об остановке). |
| **14** | Ошибка ориентации/подключения | Подаём «+», на выходе «−» (и наоборот) → уведомление о перепутанной полярности/неисправности датчика. **На точку X=0 не распространяется** (там знак не определён). |
| **27** | Рост охлаждения с током | Задержка охлаждения растёт с током. Алгоритм — предположительно ∝ I² (джоулево тепло) с потолком; подключается галочкой, помечается **BETA**. |

**Риск: высокий** (много комбинаторики), но **проверяемо без железа** — в этом
и смысл вынесения планировщика. Ожидаю здесь основную массу новых тестов.
**Оценка:** 3-4 подхода.

---

### Ф3 — Данные, отчёты, графики · пункты 31, 10, 21, 20, 22, 30, 36, 26

Можно начинать параллельно с концом Ф2 (зависит только от А-3).

| № | Что | Ключевое |
|---|---|---|
| **31** | Знаковая погрешность | Убрать `np.abs` в `analysis.py:91`. Затрагивает график, сводку, отчёт — везде, где сейчас предполагается неотрицательность. |
| **10** | Определение коэффициента | Отдельный режим: считает фактический коэффициент по снятым точкам (МНК) и показывает **и факт, и округление кратно 50** с процентом расхождения (см. В-4). |
| **21** | XLSX | Полная копия CSV + оформление. Лист 1 — raw data, лист 2 — мета. Выбор папки и имени. Основа — `report.py` из DTCal (там уже есть форматирование, ширины колонок, закреплённая шапка). |
| **20** | График из файла | Вкладка «График» умеет открывать любой CSV, по умолчанию — последний. |
| **22** | График в цикле | Вернуть построение в измерительный цикл (убрано в ветке deepseek): по окончании график сохраняется и открывается автоматически. Не ломая п. 20. |
| **30** | Подписи погрешности | Галочка «указывать погрешность над точкой», подписи **вертикальные** (чтобы не налезали), точность до 0.001 %. |
| **36** | Диапазоны и доработка вкладки | Настраиваемые диапазоны осей + приведение вкладки «График» в порядок. Состав доп. функционала уточняется здесь же (масштаб, сетка, экспорт, сравнение файлов — предложу список отдельно). |
| **26** | Правка точек | Выбор и исключение плохих точек прямо в UI. Исключение **не удаляет** сырьё, а помечает — с возможностью вернуть. Сюда же ложится пост-обработка `invert_input` из Ф0. |

**Риск: средний.** **Оценка:** 2-3 подхода.

---

### Ф4 — Служба приборов · пункты 25, 12, 11, 13

Архитектура А-4. Отделяемо и параллелится с Ф3.

| № | Что |
|---|---|
| **25** | Асинхронный сервис обнаружения: не переискивать приборы на каждый запуск, следить за изменением подключений в фоне, показывать в UI число найденных VISA-устройств и индикатор реле. Первый запуск измерения всё равно прогоняет полную проверку. |
| **12** | Поля «мультиметр»/«источник» — **выпадающий список найденных устройств**, а не пустое поле для ручного ввода VISA-адреса. |
| **11** | `blink` — прибор как-то себя обозначает по VISA. Объединяется с п. 12: выбрал в списке — нажал «мигнуть» — увидел, какой это физически. |
| **13** | Ручное управление реле: нажал — зафиксировалось в нужном положении. Отдельно от измерительного цикла. Обязательно уважает лимиты п. 28 и аварийный останов п. 32. |
| **40** | Прямая уставка значения со знаком: реле и источник настраиваются сами (см. §1). Использует ту же логику «знак → реле», что планировщик из Ф2. Нужен и в GUI, и в CLI. |

**Риск: средний** (асинхронность + железо). **Оценка:** 2 подхода.

---

### Ф5 — UI/UX и CLI · пункты 33, 24, 15, 16, 23, 34, 3-UI, 35

| № | Что |
|---|---|
| **33** | Общая переработка UI: эстетика, компоновка, читаемость. Делается **после** Ф1-Ф4, когда состав элементов уже устоялся — иначе красить придётся дважды. |
| **24** | Окно при запуске развёрнуто на весь экран (maximized), **не** fullscreen. |
| **15** | Обратный отсчёт мм:сс + расчётное время окончания по системным часам. Оценка = f(число точек, усреднение, задержки, охлаждение). Только GUI; **CLI не трогаем — тогда Linux/macOS от этого не ломаются** (п. 34). |
| **16** | Режим предупреждения: баннер почти на весь экран, мигающий восклицательный знак, красное на белом, «ВНИМАНИЕ! Идут измерения! Приближаться к измерительному стенду ОПАСНО!». **Только по ручной активации.** На самом баннере — **большая кнопка «СТОП»**: баннер закрывает почти весь экран, и если единственная кнопка останова осталась под ним, режим безопасности превращается в свою противоположность. Кнопка вызывает тот же аварийный останов, что и обычная (п. 32). |
| **23** | Рабочая папка настраивается из UI. Конфиги остаются в установочной/конфигурационной директории (см. В-1), кеш — в `<рабочая папка>\Cache`. |
| **3-UI** | Редактор конфигов приборов в UI, включая обновление даты поверки. |
| **34** | Ревизия CLI: все фичи, кроме сугубо UI-шных (15, 16, 26, 33), доступны флагами. Регрессионные тесты CLI. |
| **35** | Финальная расстановка пометок **BETA**: источники напряжения, выход в вольтах, адаптивное охлаждение (27), автоопределение коэффициента (10). Enforce 60 В. |
| **38** | Галочка «Отключить все предупреждения и уведомления (не рекомендуется)» рядом с существующей про самотесты. Жёсткий запрет 800 А и аварийный останов ею **не** отключаются (см. §1). |
| **39-UI** | Выпадающий список профилей датчиков вместо текстового поля, кнопки Сохранить/Загрузить/Переименовать/Удалить. |

**Риск: низкий-средний.** **Оценка:** 3 подхода.

---

### Ф6 — Дистрибуция · пункты 1, 2

Делается последней: пока состав файлов и путей не устоялся, инсталлятор
переписывать придётся каждый раз.

| № | Что |
|---|---|
| **1** | Инсталлятор. Установка без прав администратора (см. В-1), ярлык на рабочий стол, при возможности — закрепление в «Пуск». Логотипы — от заказчика, вставляются готовыми. |
| **2** | Автообновление. Источник — релизы GitHub с ассетом-меткой `win64`. Ссылка на репозиторий — в файле конфигурации. Объединяется с инсталлятором в **Legoshi Updater**. Требует, чтобы установка была в пользовательский профиль, иначе апдейтеру нужен UAC на каждое обновление. |

**Риск: средний** (новая для проекта область). **Оценка:** 2-3 подхода.

---

## 4. Сводная карта пунктов ТЗ → фаз

| Ф0 | Ф1 | Ф2 | Ф3 | Ф4 | Ф5 | Ф6 |
|---|---|---|---|---|---|---|
| мёрдж deepseek | **32** ✅, 28, 6, 4, 5, 3 | 17, 18, 8, 19, 29, 9, 7, 14, 27, **37**, **39** | 31, 10, 21, 20, 22, 30, 36, 26 | 25, 12, 11, 13, **40** | 33, 24, 15, 16, 23, 34, 3-UI, 35, **38**, **39-UI**, **40-CLI** | 1, 2 |

Все 40 пунктов распределены. Пункты 3, 35, 39 и 40 расщеплены между фазами
(логика в Ф1/Ф2/Ф4, интерфейс и пометки в Ф5) — это отмечено явно.

## 5. Порядок выпуска

Не копить всё до одного релиза v2.0 — на 36 пунктах это гарантированный
способ получить неотлаживаемый ком. Предлагаю:

- **v2.0-alpha** после Ф1 — приборы и безопасность. Уже полезно само по себе
  (заработают датчики напряжения, починится ранжирование), тестируется на стенде.
- **v2.0-beta** после Ф3 — вся измерительная и отчётная функциональность.
- **v2.0** после Ф6 — полный комплект с инсталлятором и автообновлением.

Каждая фаза — отдельный набор коммитов в `v2-dev`, тест-сьют зелёный на
каждом шаге, в `main` не льём до v2.0-alpha.

## 6. Ход работ

- [x] **Ф0** — мёрдж `test_deepseek_hermes` в `v2-dev`, починка 4 дефектов
      ветки, 102/102 тестов зелёные (коммит `5a103c5`).
- [x] **Ф1** — приборы и безопасность (все пункты закрыты, см. подпункты ниже):
  - [x] п. 32 — аварийный останов (`safety.py`, `SessionHandle`, коммит `e6d2985`);
  - [x] п. 28 — лимиты тока реле (`limits.py`, коммит `920cd97`): 800 А
        жёсткий запрет, >400 А предупреждение (не блокирует), заодно
        паспортные пределы источника (10 В/1020 А АКИП-1162) в конфиге и
        в проверке;
  - [x] п. 6 — починка ручного ранжирования (`instruments.py`, `measurement.py`):
        все 5 дефектов из §0.3 плюс поддержка `{index}`-стиля DM3068;
  - [x] п. 4 — мультиметр как вольтметр: новая директория
        `instruments/multimeters_voltage/` (отдельно от `multimeters/` —
        см. апдейт §0.2 ниже, зачем именно отдельная папка, а не
        переключение режима в тех же файлах), три конфига из IDM-DTCal,
        `Multimeter.measure()`/`measure_voltage()`, `parse_scpi_number()`.
        **Граница, за которую сознательно не зашёл**: измерительный цикл
        (`measurement.py`) по-прежнему всегда трактует выход датчика как
        ток (`measure_current()`, колонка `I_meas_A`) — переключение на
        реальный вольтметровый выход требует оси А-1 (тип выхода датчика
        как независимый параметр), это Ф2/Ф3, не Ф1. Здесь — только
        доказанная работоспособность самого класса `Multimeter` в роли
        вольтметра (тесты), без интеграции в измерительный цикл.
  - [x] п. 5 — починка источника напряжения (`instruments.py`, `VoltageSource`):
        `output_on/off` теперь используют ALLOUTON/ALLOUTOFF (приоритетно,
        с fallback на `:OUTPut{ch}:STATe` для гипотетических других
        моделей), `measure_voltage()`/`measure_current()` добавлены с
        нуля через `parse_scpi_number()` (readback не существовал вовсе —
        поля `measure_voltage_command`/`measure_current_command` были
        мёртвыми в конфиге), `baud_rate: 115200` из конфига применяется к
        ASRL-сессии. Плюс — паспортный потолок 64 В теперь проверяется в
        `cli.validate_measure_params` (см. п.0.4: "нигде не проверяется"
        было открытым долгом с самого начала Ф1).
  - [x] п. 3 — даты поверки приборов: `calibration.py` (чистая логика,
        не блокирует измерение ни при каком статусе — решение оператора),
        подключено в `orchestrate.py` — уведомление в лог при открытии
        приборов для любого статуса, кроме OK: тихо только когда поверка
        действительно в порядке; UNKNOWN («дата поверки не указана в
        конфиге») отмечается пометкой ℹ, DUE_SOON/OVERDUE — ⚠. Плюс
        метаданные поверки в шапку CSV. **Реальные даты поверки НЕ
        вписаны** ни в один конфиг — их не из чего было взять, придумывать
        метрологические даты недопустимо; сейчас для всех приборов статус
        UNKNOWN, и оператор об этом явно уведомляется при каждом запуске.
        Редактор в UI — Ф5 (`3-UI`), как и планировалось.
        **Сокращение относительно В-2**: S/N в шапку CSV не попал — *IDN?
        сейчас опрашивается транзитно и нигде не сохраняется, формат
        ответа у каждого вендора свой; не стал приплетать парсинг S/N к
        этому же коммиту, это отдельная небольшая задача.
- [x] **Ф2** — ядро измерения (все пункты закрыты):
  - [x] **А-2, планировщик развёртки** (`sweep.py`, новый модуль): чистая
        функция `plan_sweep()` без обращения к железу — вся комбинаторика
        знаков/направлений/пресетов здесь, измерительный цикл стал тупым
        исполнителем плана, как и было задумано ("ключевое решение всего v2").
        42 юнит-теста, каждый кейс сверен через прямой запуск интерпретатора
        перед написанием ассертов (не наугад).
  - [x] п. 17 — универсальный ввод: любые X_start/X_stop, любой знак и
        порядок (`-250→250`, `0→250`, `250→-250`, `-25→25`, `150→250`, ...).
        Ноль — только если развёртка через него реально проходит. Модуль
        конечной точки — всегда, точно, без сноса плавающей точкой.
        Заодно снята блокирующая валидация `X_stop < X_start` в
        `cli.validate_measure_params`, которая до этого коммита ВСЁ ЕЩЁ
        запрещала ровно то, что должен был разрешить этот пункт.
  - [x] п. 18 — баги шага: шаг через ноль, шаг больше диапазона, шаг не
        кратный диапазону, накопление ошибки float — всё закрыто
        конструкцией `_raw_pass()` (точки считаются от start независимо,
        не приращением; конечная точка форсируется точным значением).
  - [x] п. 8 — `sweep.Branch` (`positive`/`negative`/`both`) заменяет
        булев `use_relay` по всему стеку (cli.py/gui.py/orchestrate.py/
        measurement.py). Однозначно решено: "без реле вообще" (ручная
        коммутация) в это перечисление не входит — это домен п.13/40 (Ф4),
        не сводки развёртки.
  - [x] п. 19 — 4 именованных пресета (`DirectionPreset`), включая
        обязательный "сходящийся через ноль" (converging). Выяснилось по
        ходу: DIVERGING/CONVERGING делят один общий ноль (как в v1.4ae),
        DESCENDING/FULL_CYCLE намеренно возвращаются в ноль по нескольку
        раз — это и есть смысл снятия петли гистерезиса, ноль после разных
        экскурсий может показывать разное.
  - [x] п. 37 — витки: `turns` в `run_measurement()`, `X_real = X_set × turns`
        (знаковый — нашёл и исправил свою же ошибку через собственный тест:
        сначала посчитал от `magnitude`, потеряв знак). Ожидаемый выход и
        лимиты реле — раздельно: первый от реального входа, вторые от
        `X_set` (через провод), как и требовалось.
  - [x] п. 29 — усреднение: `averaging_count`/`averaging_delay`/`discard_first`,
        по умолчанию 4/0/True.
  - [x] п. 9 — контрольные промеры: до 3 попыток на точку при выходе за
        `error_threshold`, целиком включая новый заход усреднения.
  - [x] п. 7 — отсечка по погрешности переработана: срабатывает только
        после всех попыток п.9 ("решение об остановке после механики п.9"),
        не на первом же отклонении, как раньше.
  - [x] п. 14 — детект перепутанной полярности: знак показания не совпадает
        со знаком уставки → `PolarityMismatch` в результате и в логе. На
        X=0 не распространяется (там своя, более простая ветка).
  - [x] п. 27 — адаптивное охлаждение (BETA): квадратичный рост с током
        (джоулево тепло ∝ I²) с потолком `adaptive_cooling_max_multiplier`,
        выключено по умолчанию, явная пометка BETA везде (CLI/GUI/CSV).
  - [x] п. 39 — `SensorConfigManager` доведён до состояния из плана:
        переезд в `apppaths.sensor_config_dir()` (`%LOCALAPPDATA%\Legoshi\
        IDM\IVTrace\config\sensors`, вне рабочей папки), подпапки
        `current`/`voltage`, белый список безопасных символов в имени
        (никаких `.`/`/`/`\\` — структурно невозможен обход `../..`),
        метаданные `_meta` (дата, версия, комментарий). Заодно найден и
        починен реальный, независимо от этого пункта существовавший баг:
        `run.py` импортировал `SensorConfigManager`, но никогда не
        создавал и не передавал его в `resolve_measure_params()` — флаги
        `--save-config`/`--load-config` были полностью мертвы в CLI с
        самого мёрджа ветки deepseek (Ф0), работали только из GUI.
  - [x] Заодно (обнаружено собственным тестом при рефакторинге, не было в
        плане явно): `load_and_analyze()` в `analysis.py` не знал про
        колонку `Rejected` и включал забракованные п.9 точки в сводную
        погрешность — прямое противоречие формулировке п.9 ("не идёт в
        расчёт"). Исправлено: `Rejected` исключаются из статистики
        (`max_error_percent`/`mean_error_percent`), но остаются в
        `dataframe` целиком — как сырые данные и требовалось.
  - Сквозная проверка: воспроизведён ИМЕННО исходный сценарий issue #3
    (0→50→100 А, датчик 1:2000) на полном стеке `run_measurement()` с
    честной симуляцией переполнения диапазона — ни одного значения
    порядка 1e38, обе точки посчитаны верно (0.025 А / 0.050 А).
  - 308/308 тестов (было 286 после Ф1).
- [x] **Ф3** — данные, отчёты, графики (все пункты закрыты):
  - [x] п. 31 — знаковая погрешность: `np.abs` убран из `analysis.py`.
        Колонка `Error_percent` теперь знаковая по всей оси. Сводный
        `max_error_percent` — не арифметический максимум (тот терял бы
        крупные отрицательные отклонения), а знаковое значение ИМЕННО в
        точке с наибольшим отклонением ПО МОДУЛЮ; `mean_error_percent` —
        знаковое среднее (систематическое смещение видно по знаку).
  - [x] п. 10 — определение коэффициента (BETA): `estimate_ratio_from_data()`,
        МНК через ноль (`I_meas = X_set / X_actual`), округление кратно 50
        (мин. 50) плюс процент расхождения факт/округление, как решено в
        В-4. Кнопка в GUI (отдельно от построения графика) и
        `analyze --estimate-ratio` в CLI.
  - [x] п. 21 — XLSX: `export_xlsx()` на `openpyxl` (добавлен в
        `requirements.txt`) — лист "Данные" с закреплённой шапкой и
        подобранными ширинами колонок, лист "Метаданные" из тех же
        `#`-строк CSV. Выбор пути — `filedialog.asksaveasfilename` в GUI,
        `analyze --xlsx` в CLI.
  - [x] п. 20 — график из файла: кнопка «Открыть CSV…» на вкладке «График»
        (`filedialog.askopenfilename`), предзаполняет I ном./коэффициент из
        собственной шапки метаданных открытого файла через
        `metadata_i_nom_and_ratio()` — не тащит значения от предыдущего
        файла. По умолчанию (файл не открывали явно) — последний CSV в
        рабочей папке, как раньше.
  - [x] п. 22 — график в цикле: `analysis.load_and_analyze_from_params()` —
        общая обёртка, которой пользуются и CLI (`cmd_measure`, открывает
        PNG через `os.startfile`), и GUI (`_auto_plot_after_measurement`,
        вызывается из обработчика события "done" на главном потоке Tk —
        сборка фигуры/её встраивание в Tk принципиально не делается в
        рабочем потоке измерения). Без I ном./коэффициента в параметрах —
        тихий пропуск с пометкой в журнале, это не ошибка измерения.
        Ручной режим (п.20) не задет: то же самое API.
  - [x] п. 30 — подписи погрешности: галочка на вкладке «График», подписи
        вертикальные (`rotation=90`), формат `.3f%` (точность 0.001%).
  - [x] п. 36 — диапазоны осей (X общий + Y верхнего/нижнего графика,
        галочка "авто" по умолчанию включена) плюс общая пересборка вкладки
        «График» по секциям (Labelframe): файл / параметры анализа /
        отображение / точки / экспорт — вместо одной строки вперемешку.
        Полная эстетическая доводка — п.33, сознательно оставлена Ф5.
  - [x] п. 26 — правка точек: таблица (`ttk.Treeview`) со всеми точками
        текущего файла, «Исключить/Вернуть выбранные» переключает колонку
        `ManuallyExcluded` (аналогично `Rejected` — учитывается в фильтре
        статистики `analysis.py`, но строка остаётся в `dataframe` целиком),
        «Сохранить и перестроить» пишет через `save_dataframe_with_metadata()`
        (шапка CSV сохраняется как есть) и сразу перестраивает график.
        Сюда же перенесена пост-обработка `invert_input` из Ф0
        (`apply_invert_input()`): теперь это явная операция над уже снятым
        файлом, пишет НОВЫЙ `*_inverted.csv` рядом (исходные данные не
        трогает), с пометкой в его шапке и подтверждающим диалогом
        «не рекомендуется».
  - Заодно найден и починен независимый от плана баг в GUI: вкладка
    «График» создавала СВОИ Entry для «I ном.»/«Коэфф. 1:X» и присваивала их
    тем же атрибутам `self.e_inom`/`self.e_ratio`, что и панель параметров
    измерения (`_build_right()` вызывается после `_build_params()` и
    перезаписывает атрибут) — поля в панели параметров были видны на
    экране, но ничего не читало то, что в них вводили: и сохранение
    конфига датчика, и сбор параметров измерения на самом деле брали
    значения из виджета на вкладке «График». Разведены на `self.e_inom`
    (панель параметров) и `self.plot_e_inom`/`self.plot_e_ratio` (вкладка
    «График», уже независимо от коэффициента, с которым велось измерение —
    так и нужно для п.20, открытие произвольного старого файла).
  - Заодно (не в плане явно, но всплыло при первом реальном прогоне
    тестов в этой рабочей директории): окружение оказалось совсем без
    зависимостей проекта (даже pandas/pytest не были установлены) — при
    установке `pip` уткнулся в системный SOCKS-прокси
    (`socks=127.0.0.1:10808` в настройках Windows) без `PySocks`; заодно
    всплыл отдельный баг, не связанный с Ф3: справка CLI-флага
    `--error-threshold` в `cli.py` содержала непойманный литеральный `%`,
    из-за чего под Python 3.14 (более строгая проверка `argparse` строк
    справки) падал сам разбор параметров — почти наверняка был бы невидим
    и дальше, пока кто-то не собрал бы проект именно на 3.14.
  - 328/328 тестов (было 308 после Ф2).
- [ ] Ф4 … Ф6 — см. выше.
